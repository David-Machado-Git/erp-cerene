import { reactive } from "vue";

// Cria um Event Bus reativo
export const eventBus = reactive({ colab: null });

export const unidadeEvenBus = reactive({ unidade: null }) as any;




// export const eventBus = reactive({
//   colab: null as Colab | null
// });

